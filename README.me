This server was creted in order to work the basics on graphql and to help
learning on how to integrate it with React.

Scripts:

In the project directory, you can run:

npm run json-server

Simulates a  backEnd server API located on db.json, (localhost: 3000/Persons)

on the actual commit need to uncomment axios in order to make the fetch.


-----------------

node index.js

In order to start the server with an static database